<% if src_image.work_in_progress %>
    <%= image_tag 'under_construction.png' %>
<% else %>
    <%= link_to thumbnail(src_image.src_thumb),
                :controller => :gend_images,
                :action => :new,
                :src => src_image.id_hash %>
<% end %>
